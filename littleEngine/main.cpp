#include "mainwindow.h"
#include <QApplication>
#include "../PBR_d12/PBR_d12.h"


int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    MainWindow w;
	w.
	PBRD12 win((HWND)w.winId());
	(HWND)w.winId();
    w.show();

    return a.exec();
}
